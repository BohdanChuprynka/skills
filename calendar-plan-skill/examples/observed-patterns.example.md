# Observed Patterns

Append-only defaults inferred from past calendar history, read by the
`calendar-plan` skill on each run (canonical location:
`~/.config/calendar-plan/observed-patterns.md`). Keep `SKILL.md` generic; this
file holds learned, personal defaults. The planner treats these as soft
defaults, not hard constraints — the existing fixed calendar always wins.

> **Copy this file** to `~/.config/calendar-plan/observed-patterns.md` and edit.
> The real file is gitignored. It is also updated over time as patterns emerge.

## Daily rhythm

- Wake / start-of-day: `<HH:MM>`
- Wind-down / end-of-day: `<HH:MM>`

## Recurring blocks (typical slots, not fixed events)

- Gym: `<days>` around `<HH:MM>`, `<duration>` min
- Deep-work block length: `<N>` min
- Meals: breakfast `<HH:MM>`, lunch `<HH:MM>`, dinner `<HH:MM>`

## Commute / transitions

- Commute assumption between `<place A>` and `<place B>`: `<N>` min

## Notes

- Add observed defaults here as they become clear from history. One bullet per
  pattern; date-stamp anything that may drift.
