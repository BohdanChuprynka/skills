interface:
  display_name: "Calendar Plan"
  short_description: "Plan today from calendar, Notion, and Gmail"
  default_prompt: "Use $calendar-plan to draft a practical plan for today from my calendar, Notion task page, Gmail, and current notes."

policy:
  allow_implicit_invocation: true
