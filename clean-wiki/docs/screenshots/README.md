# Screenshots

Drop captures here, named:

- `01-preflight.png` — pre-flight screen with categories collapsed
- `02-preflight-expanded.png` — one category expanded with per-file checkboxes + Obsidian deep-link buttons
- `03-card-swipe.png` — a judgment card mid-review (stale_fact or orphan) with the diff block visible
- `04-summary.png` — summary screen after review

Then uncomment the `<!-- ... -->` block in the top-level README.md.

## How to capture

macOS: `Cmd+Shift+4`, then drag to select the browser window. Saves to Desktop. Move into this folder + rename.

Suggested viewport: 1400×900 (close to what shows in the screenshots already in repo).
