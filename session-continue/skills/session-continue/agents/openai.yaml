interface:
  display_name: "Session Continue"
  short_description: "Continue Claude Code or Codex sessions by id (source auto-detected)."
  default_prompt: "Use $session-continue <session-id> -- continue the task."
