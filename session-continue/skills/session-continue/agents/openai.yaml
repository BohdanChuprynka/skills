interface:
  display_name: "Session Continue"
  short_description: "Import Claude Code or Codex sessions by id."
  default_prompt: "Use $session-continue from claude <session-id> -- continue the task."
